<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/header') %>
<body>
    <h1 class="text-2xl bg=blue-700">Post</h1>
    <ul>
        <% posts.forEach(post => { %>
            <li><strong><%= post.title %></strong><br><%= post.body %></li>
        <% }) %>
    </ul>

</body>
</html>